<!doctype html>

<div class= "proj">
	{{#each project}}
	<div class="project">
		<div class= "projBlurbs">
		<h3>{{Title}}</h3>
		<p>{{Subtitle}}</p>
		</div>
		<div>
		<a href="{{id}}.html">Read More</a>
		</div>		
	</div>
	{{/each}}
</div>	