<!doctype html>

<div class = "col-lg-8">
	{{#each project}}
	<div class="project">
		<div class= "projBlurbs">
		<h3>{{Title}}</h3>
		<p>{{Subtitle}}</p>
		<a href="{{id}}.html">Read More</a>
		</div>		
	</div>
	{{/each}}
</div>	