<!doctype html>
	<div>
	<a href="javascript:history.back()" class="link"><h3><- BACK</h3></a>
	</div>
	<div class="proj_title">
		<h1>{{project}}</h1>
		<div class="proj_details">
			<h5>ROLE: DESIGNER TIME FRAME: 4 WEEKS</h5>
			<h5>ROLE: DESIGNER TIME FRAME: 4 WEEKS</h5>
		</div>
	</div>
	<div class="background full_photo">
		<img src="./public/images/{{image}}">
	</div>
	<h4>design process</h4>
	{{#each category}}
	<div class="row">
		<div class="col-lg-5">	
			<h3> {{Title}}</h3>
		</div>
		<div class="col-lg-7">
			<p>{{Paragraph}}</p>
		</div>
		{{#if image}}
		<div class="background full_photo">
			<img src="./public/images/{{image}}">
		</div>
		{{/if}}
	</div>
	{{/each}}